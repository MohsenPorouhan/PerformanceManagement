@{
    Layout = null;
}
@model List<ScoreView>
@if (ViewBag.type == 1)
{
    <table class="table table-striped table-bordered table-hover" id="sample_dt">
        <thead>
            <tr>

                <th id="th1">
                    نام شایستگی
                </th>

                <th id="th2">
                    نمره مربی سطح1
                </th>
                @if (ViewBag.hasParticipant > 0 && ViewBag.participantConfirmation ?? false)
                {
                    <th id="th4">
                        نمره سایرارزیاب
                    </th>
                }
                <th id="th5">
                    خود ارزیابی
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>

                    <td>@item.Title</td>

                    <td>@item.score1</td>

                    @if (ViewBag.hasParticipant > 0 && ViewBag.participantConfirmation ?? false)
                    {
                        <td>@item.participantScore</td>
                    }
                    <td>@item.selfScore</td>
                </tr>
            }

        </tbody>
    </table>
}
else if (ViewBag.type == 2)
{
    <table class="table table-striped table-bordered table-hover" id="sample_dt2">
        <thead>
            <tr>
                <th id="th1">
                    نام شایستگی
                </th>
                <th id="th2">
                    نمره مربی سطح1
                </th>
                <th id="th3">
                    نمره مربی سطح2
                </th>
                @if (ViewBag.hasParticipant > 0 && ViewBag.participantConfirmation ?? false)
                {
                    <th id="th4">
                        نمره سایرارزیاب
                    </th>
                }
                <th id="th5">
                    خود ارزیابی
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Title</td>
                    <td>@item.score1</td>
                    <td>@item.score2</td>

                    @if (ViewBag.hasParticipant > 0 && ViewBag.participantConfirmation ?? false)
                    {
                        <td>@item.participantScore</td>
                    }
                    <td>@item.selfScore</td>
                </tr>
            }

        </tbody>
    </table>
}
else if (ViewBag.type == 4)
{
    <table class="table table-striped table-bordered table-hover" id="sample_dt3">
        <thead>
            <tr>
                <th id="th1">
                    نام شایستگی
                </th>
                <th id="th2">
                    نمره مربی سطح @ViewBag.coacherLevel
                </th>
                @if (ViewBag.hasParticipant > 0 && ViewBag.participantConfirmation ?? false)
                {
                    <th id="th4">
                        نمره سایرارزیاب
                    </th>
                }
                <th id="th5">
                    خود ارزیابی
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Title</td>
                    @switch (ViewBag.coacherLevel)
                    {
                        case 2:
                            <td>@item.score2</td>
                            break;
                        case 3:
                            <td>@item.score3</td>
                            break;
                        case 4:
                            <td>@item.score4</td>
                            break;
                        default:
                            break;
                    }


                    @if (ViewBag.hasParticipant > 0 && ViewBag.participantConfirmation ?? false)
                    {
                        <td>@item.participantScore</td>
                    }
                    <td>@item.selfScore</td>
                </tr>
            }

        </tbody>
    </table>
}
else if (ViewBag.type == 5)
{
    <table class="table table-striped table-bordered table-hover" id="sample_dt3">
        <thead>
            <tr>

                <th id="th1">
                    نام شایستگی
                </th>

                <th id="th2">
                    نمره مربی سطح @ViewBag.coacherLevel
                </th>
                @if (ViewBag.hasParticipant > 0 && ViewBag.participantConfirmation ?? false)
                {
                    <th id="th4">
                        نمره سایرارزیاب
                    </th>
                }
                <th id="th5">
                    خود ارزیابی
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Title</td>

                    @switch (ViewBag.coacherLevel)
                    {
                        case 2:
                            <td>@item.score2</td>
                            break;
                        case 3:
                            <td>@item.score3</td>
                            break;
                        case 4:
                            <td>@item.score4</td>
                            break;
                        default:
                            break;
                    }


                    @if (ViewBag.hasParticipant > 0 && ViewBag.participantConfirmation ?? false)
                    {
                        <td>@item.participantScore</td>
                    }
                    <td>@item.selfScore</td>
                </tr>
            }

        </tbody>
    </table>
}
